{
  // 时区
  "timeZone": "Asia/Shanghai",
  "app": {
    // GitHub App ID
    "id": "3092456",
    // GitHub App 私钥
    // 可以是路径 或 内容
    "privateKey": "./key/kawaii-latedreambot.2026-03-14.private-key.pem",
    // 使用的语言包
    "locale": "zh-CN",
    // 命令配置
    "cmd": {
      // 命令前缀
      "prefix": "/",
      // 权限等级要求
      // any: 任何人
      // bot: bot / contributor / collaborator / owner
      // contributor: contributor / collaborator / owner
      // collaborator: collaborator / owner
      // owner: owner
      "permissionLevel": "collaborator"
    }
  },
  "server": {
    // 服务器端口
    "port": 429,
    // Webhooks 密钥
    "secret": "dev-test-secret",
    // 日志级别
    "logLevel": "info"
  },
  "webui": {
    // WebUI 端口
    // TODO
    "port": 430
  }
}
